---
title: "End-to-end encryption"
description: "Implement Ave app-key handoff safely, avoid URL-fragment decoding pitfalls, and isolate identity data correctly."
---

Ave can deliver a per-app, per-identity encryption key for E2EE-capable apps.

Ave also supports **shared secret transfers** for app-scoped or global secrets that need to move between identities. Use the normal app-key handoff for your app's base encryption context, and use shared-secret transfers when you need to grant another identity access to an additional secret such as a vault root or workspace key.

## How key handoff works

- App is marked `supportsE2ee`
- During the consent step, the Ave authorization UI decrypts the server-stored encrypted key using the user's master key
- The plaintext app key is passed to your callback as a URL fragment (`#app_key=...`) — never in the JSON token response
- Parse and clear the fragment before or alongside the authorization code exchange

## Storage model

- Key is identity-specific
- Same identity gets same app key across logins
- Different identities must map to separate encrypted data domains
- Shared secrets are also identity-specific after claim, even when they originated from another identity

## URL-fragment parsing gotcha

When parsing fragment with `URLSearchParams`, `+` may become space. Normalize before decoding.

```ts
const hashParams = new URLSearchParams(window.location.hash.slice(1));
const appKeyRaw = hashParams.get("app_key");
const appKeyBase64 = appKeyRaw?.replace(/ /g, "+");
```

Then clear sensitive fragment from history.

```ts
window.history.replaceState({}, document.title, window.location.pathname + window.location.search);
```

## Crypto recommendations

- Import app key with Web Crypto
- Use AES-GCM with random 96-bit IV per encryption
- Store `{iv + ciphertext}` together
- Never reuse IV with same key

## Failure modes to handle

- Missing key on first login for E2EE app (treat as setup failure)
- Identity switch returns different key context
- Key decode failures due to base64 normalization bug
- Fragment key left in browser history/logs
- Shared secret claim authenticated as the wrong handle
- Recipient identity missing an Ave encryption key

## Shared secret transfers

If your app needs to share a secret with another Ave identity:

- look up the recipient encryption key by handle
- encrypt the secret locally to that public key
- create a transfer contract in Ave
- send the claim link to the recipient
- let the recipient claim and store their own wrapped copy

See:

- [Shared secret transfers](/guides/shared-secret-transfers)
- [Shared secret endpoints](/api/shared-secret-endpoints)
